/*
 * Intentionally empty.
 *
 * Firebase Hosting rewrites requests for organizer/event scripts that do not
 * exist to this file, so EventSystem can always include the script tag and
 * get a 200 response with an empty body instead of a 404.
 */
